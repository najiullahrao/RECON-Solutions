# Backend API base URL (no trailing slash).
# Leave unset in dev to use Vite proxy (http://localhost:5173/api -> http://localhost:5000).
# Set for production or to point at a specific backend:
# VITE_API_URL=http://localhost:5000
# VITE_API_URL=https://your-backend.example.com
